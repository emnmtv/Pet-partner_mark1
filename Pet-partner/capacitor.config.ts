import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'pet-partner',
  webDir: 'dist/pet-partner/browser'
};

export default config;
